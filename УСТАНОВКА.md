# 🚀 Установка сайта «Соль и Перец» на свой хостинг

---

## ⚡ ВАРИАНТ 1 — Vercel (САМЫЙ ПРОСТОЙ, бесплатно)

Сайт уже задеплоен на Vercel. Просто подключите свой домен:

1. Зайдите на https://vercel.com и войдите
2. Найдите проект **sol-i-perec-cafe**
3. Нажмите **Settings** → **Domains**
4. Нажмите **Add** → введите ваш домен (например `soliperec.ru`)
5. Vercel покажет две DNS-записи — добавьте их у регистратора домена:
   - Тип **A** → IP-адрес от Vercel
   - Тип **CNAME** → адрес от Vercel
6. Подождите 10–30 минут — сайт заработает на вашем домене

✅ База данных (Supabase) остаётся та же, ничего не нужно переносить.

---

## 🖥️ ВАРИАНТ 2 — VPS (Timeweb, REG.RU, Beget Cloud, Selectel)

### Требования к серверу
- Ubuntu 20.04 / 22.04
- Node.js 18+
- 512 MB RAM минимум
- Nginx (для домена и SSL)

### Шаг 1 — Установите Node.js на сервере

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
node --version   # должно быть v20.x.x
```

### Шаг 2 — Установите PM2 (автозапуск)

```bash
sudo npm install -g pm2
```

### Шаг 3 — Загрузите файлы на сервер

**Способ А — через Git (если код в репозитории):**
```bash
git clone ВАШ_РЕПОЗИТОРИЙ /var/www/soliperec
cd /var/www/soliperec
```

**Способ Б — через FTP/SFTP (FileZilla):**
- Скачайте все файлы проекта на компьютер
- Подключитесь через FileZilla к серверу
- Загрузите папку в `/var/www/soliperec`

### Шаг 4 — Создайте файл с переменными окружения

```bash
cd /var/www/soliperec
nano .env
```

Вставьте содержимое (Ctrl+Shift+V), сохраните (Ctrl+X, Y, Enter):

```
NEXT_PUBLIC_SUPABASE_URL=https://ylfaprsqkzcgzeizpmsc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_RKd3PCmrhnMfo9wxjAyoxQ_Rr0fwkfp
SUPABASE_SERVICE_ROLE_KEY=sb_secret_v0ck2wz6f7jH5_DIHOTR5Q_M3SPwBPO
VITE_SUPABASE_URL=https://ylfaprsqkzcgzeizpmsc.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_RKd3PCmrhnMfo9wxjAyoxQ_Rr0fwkfp
ADMIN_PASSWORD=soliperec2025
PORT=3000
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН (если нужны уведомления)
TELEGRAM_CHAT_ID=ВАШ_CHAT_ID (если нужны уведомления)
```

### Шаг 5 — Установите зависимости и соберите

```bash
cd /var/www/soliperec
npm install
npm run build
```

### Шаг 6 — Запустите через PM2

```bash
pm2 start ecosystem.config.cjs
pm2 save
pm2 startup   # скопируйте и выполните команду которую он покажет
```

Проверьте что работает:
```bash
pm2 status
curl http://localhost:3000/api/health
```

### Шаг 7 — Настройте Nginx + домен + SSL

```bash
sudo apt install nginx certbot python3-certbot-nginx -y
sudo nano /etc/nginx/sites-available/soliperec
```

Вставьте конфиг (замените `ВАШ_ДОМЕН.RU` на свой домен):

```nginx
server {
    listen 80;
    server_name ВАШ_ДОМЕН.RU www.ВАШ_ДОМЕН.RU;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/soliperec /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# Получить бесплатный SSL сертификат
sudo certbot --nginx -d ВАШ_ДОМЕН.RU -d www.ВАШ_ДОМЕН.RU
```

✅ Готово! Сайт работает на вашем домене с HTTPS.

---

## 📦 ВАРИАНТ 3 — Хостинг с Node.js (Beget, Timeweb)

Некоторые хостинги (Beget, Timeweb) поддерживают Node.js через панель управления:

1. В панели хостинга найдите раздел **Node.js** или **Приложения**
2. Создайте новое Node.js приложение
3. Укажите:
   - Версия Node.js: **18** или **20**
   - Стартовый файл: `server.js`
   - Порт: **3000**
4. Загрузите все файлы через FileZilla или менеджер файлов
5. Добавьте переменные окружения через панель хостинга
6. Выполните в консоли: `npm install && npm run build`
7. Запустите приложение

---

## 🔑 Важные данные

| Параметр | Значение |
|----------|----------|
| Пароль админки | `soliperec2025` |
| Адрес админки | `ВАШ_ДОМЕН/admin` |
| База данных | Supabase (облако, не нужно устанавливать) |
| Порт по умолчанию | 3000 |

---

## ❓ Частые вопросы

**Q: Нужно ли переносить базу данных?**
A: Нет! База данных (Supabase) — это облачный сервис. Она работает независимо от хостинга.

**Q: Как сменить пароль от админки?**
A: В файле `.env` измените строку `ADMIN_PASSWORD=новый_пароль`

**Q: Как настроить уведомления в Telegram?**
A: 1) Создайте бота через @BotFather, получите токен. 2) Узнайте ваш chat_id через @userinfobot. 3) Добавьте в `.env`: `TELEGRAM_BOT_TOKEN=токен` и `TELEGRAM_CHAT_ID=ид`

**Q: Сайт не запускается, ошибка порта**
A: Измените порт в `.env`: `PORT=8080` или другой свободный порт
